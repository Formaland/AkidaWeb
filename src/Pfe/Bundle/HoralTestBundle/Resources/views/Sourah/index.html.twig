{% extends 'PfeWebBundle:Backend:layout.html.twig' %}


{% block content -%}
    <h1>Sourah list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Namesourah</th>
                <th>Numsourah</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sourah_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.namesourah }}</td>
                <td>{{ entity.numsourah }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sourah_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sourah_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sourah_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
