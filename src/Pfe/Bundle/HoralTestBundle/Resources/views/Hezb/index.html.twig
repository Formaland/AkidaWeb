{% extends 'PfeWebBundle:Backend:layout.html.twig' %}


{% block content -%}


<p class="mb20"></p>

<div class="table-responsive">

    <table class="table table-bordered mb30">
        <thead>
        <tr>

            <th colspan="7">


                <h4 class="panel-title" align="center">
                    <font color="#008b8b" size="5%" align="left">
                        {{ 'sidebar.Liste ahzebs'|trans({}, 'backend') }}</font></h4>

            </th>
        </tr>
        <tr>

            <th></th>


            <th>
                {{ 'hezb.num hezb'|trans({}, 'backend') }}
            </th>


            <th>
                #
            </th>

        </tr>

        </thead>
        <tbody>
        {% for entity in entities %}


            <tr>


                <td class="table-action">

                    <a href="{{ path('hezb_edit', { 'id': entity.id }) }}">
                        <i class="fa fa-pencil"></i>
                    </a>
                    <a class="delete-row" href="">
                        <i class="fa fa-trash-o"></i>
                    </a>

                </td>
            <tr>
                <td>{{ entity.numhezb }}</td>
                <td><a href="{{ path('hezb_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>

               {#<td>
                <ul>
                    <li>
                        <a href="{{ path('hezb_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('hezb_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}